version: "3.8"

services:
    redis:
        image: redis:latest
        ports:
            - "6379:6379"
    bookmark_service:
        build: .
        ports:
            - "8080:8080"
        environment:
            - REDIS_ADDRESS=redis:6379
            - BASE_PATH=/v1
        depends_on:
            - redis
